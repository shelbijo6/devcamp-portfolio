<%= form_for(blog) do |f| %>
  <% if blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="blog-edit">
    <%= f.text_field :title, class: 'edit-blog-title', placeholder: 'Title' %>
  </div>

  <div class="blog-edit">
    <%= f.text_area :body, class: 'edit-blog-body', placeholder: 'Content' %>
  </div>

  <div class="blog-edit">
    <%= f.submit "Save", class: 'btn btn-primary' %>
  </div>
<% end %>